{
  "name": "volt-os",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "build": "pnpm -r build"
  },
  "pnpm": {
    "overrides": {
      "postcss": "^8.5.10"
    }
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vitest/coverage-v8": "4.1.10",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7"
  },
  "dependencies": {
    "pino": "^10.3.1"
  }
}
